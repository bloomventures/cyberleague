{:env/slug "clojure-sci"
 :env/language-slug "clojure"
 :env/enabled? true
 :env/runtime :runtime/sci
 :env/run-cmd "echo \"Not Implemented\""
 :env/build-cmd nil
 :env/artifact-path "bot.clj"
 :env/argv []
 :env/note "Used in development. Follows a different protocol - JSON stdin/stdout parsing is done by the system. Must return a single anonymous function that receives the context as input (already parsed, with keyworded-keys)."
 :env/status "dev-only"}
